# AI Legal Constructor

Инструмент для агентства «Мегаполис» (Мария Боголюбова): собирает комплект договоров
(аренда/продажа) из данных сделки — веб-форма → генератор → набор `.docx`. Локальное
приложение, данные не покидают компьютер.

**Source of truth требований — не этот README:**
`MAIN_ASSISTANT\knowledge\projects\legal-contract-constructor-handoff.md` (v1.1).
Решение о выборе архитектуры и Human Override по Roadmap — `MAIN_ASSISTANT\DECISIONS.md`,
запись 2026-08-21. Дословная задача и ход сборки —
`ENGINEER\workspace\2026-08-21-задача-юридический-конструктор-договоров.md`.

## Статус

**Phase 1 (MVP) + Phase 1.1 (загрузка фото/распознавание).** Реализовано: динамическая
модель Deal/Participants, логика комплекта (комиссия на каждого / основной договор / акт /
согласия по флагам), реестр шаблонов с одной активной версией на тип, 4 экрана веб-формы,
экспорт `.docx` (по одному и архивом), распознавание фото паспорта/выписки ЕГРН —
поля предзаполняются, но не сохраняются без вашей проверки/правки.

**Debug-режим Flask по умолчанию выключен** (интерактивная консоль Python на странице
ошибки — риск при реальных данных клиента). Включить для разработки:
`LEGAL_CONSTRUCTOR_DEBUG=1 python run.py`.

**Шаблоны в `templates_docx/` — тестовые заглушки, не юридический текст.** Реальные
шаблоны (6 штук — 3 аренда + 3 продажа) готовят Мария с юристом отдельно; движок проверен
на плейсхолдерах и примет реальные `.docx`-шаблоны с той же Jinja-разметкой
(`{{ address }}`, `{{ participant.full_name }}`, циклы `{% for p in participants %}`)
без изменений кода — см. «Как заменить шаблоны на реальные» ниже.

## Запуск

```bash
pip install -r requirements.txt
python scripts/make_placeholder_templates.py   # один раз — создать тестовые .docx-шаблоны
python run.py
```

Открыть в браузере: http://127.0.0.1:5001

### Распознавание фото (OCR) — отдельная установка

Работает локально через Tesseract — фото паспорта/ЕГРН никуда не отправляются:

```bash
winget install --id UB-Mannheim.TesseractOCR -e
```

Русский языковой пакет не входит в стандартную установку — положить файл `rus.traineddata`
(скачать с https://github.com/tesseract-ocr/tessdata_best) в папку `tessdata/` внутри
этого репозитория (не в `Program Files` — там нужны права администратора, а так не нужны).
Если OCR не настроен, экраны 1 и 2 работают как раньше — просто без блока «Распознать по
фото», приложение не падает.

## Проверка (сделано при сборке, можно повторить)

```bash
python scripts/smoke_test.py        # движок напрямую, без HTTP
python scripts/http_smoke_test.py   # тот же сценарий через реальные HTTP-запросы (нужен запущенный run.py)
python scripts/ocr_smoke_test.py    # OCR: рисует тестовое "фото" документа и проверяет распознавание
```

Первые два теста прогоняют пример из handoff §5: 2 собственника в долях (один в браке) +
1 покупатель → ожидаются 6 документов (3 комиссии + основной договор + акт + согласие
супруга). Совпало.

**Важно про кодировку при ручном тестировании через консоль:** если проверяете форму
не браузером, а `curl`/скриптом из Git Bash на Windows — русские буквы в параметрах
могут испортиться ещё до отправки (особенность консоли, не приложения). Проверено:
через настоящий браузер, `curl.exe` из PowerShell и Python `urllib`/`requests` кириллица
сохраняется корректно. Сервер также отклоняет заведомо некорректный `deal_type`/`side`
понятной ошибкой 400, а не тихо портит сделку.

## Как заменить шаблоны на реальные

1. Каждый реальный `.docx`-шаблон должен содержать те же плейсхолдеры, что и тестовый
   (открой любой файл из `templates_docx/` как образец разметки).
2. Положить файл в `templates_docx/`, прописать запись в `templates_docx/registry.json`
   со `status: "active"` — и перевести прежнюю запись того же `document_type`/`deal_type`
   в `"archived"` (на пару может быть только один `active` — правило §5 handoff).
3. Код менять не нужно.

## Что осознанно не сделано (Phase 1 → Phase 2/3, handoff §8)

Несколько сотрудников, роли, БД вместо файлов, мультитенантность, биллинг,
автоматическое подписание без человека в цикле, проверка на 152-ФЗ для внешнего хранения —
не входит в Phase 1.

## Открытые вопросы к юристу (не блокируют использование движка)

- Точный список оснований для `consent_flags` (когда именно обязательно согласие).
- Акт приёма-передачи — один на сделку или на каждого участника (сейчас: один на сделку).

Полный список — handoff §9.
