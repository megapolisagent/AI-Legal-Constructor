# Движок сборки договоров (scripts/generate_package.py) — установить перед первым запуском:
#   pip install -r skills/document-assembly/requirements.txt
docxtpl>=0.20

# PDF-шаг (готовый подписываемый файл рядом с .docx) — работает только на машине с
# установленным MS Word (COM-автоматизация через docx2pdf). Без Word генерация .docx всё
# равно проходит, PDF-шаг просто пропускается с явным сообщением в stderr.
docx2pdf>=0.1
pywin32>=300; sys_platform == "win32"
